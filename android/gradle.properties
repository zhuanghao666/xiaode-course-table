# Enable AndroidX because the app depends on androidx.core:core-ktx and FileProvider.
android.useAndroidX=true

# Keep Gradle/Kotlin output stable on Windows and Android Studio.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
